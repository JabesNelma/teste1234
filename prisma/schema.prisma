generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Run `docker compose up -d db && docker compose run --rm prisma npm run prisma:pull`
